---
output:
  html_document: default
  pdf_document: default
---

```{r read_csv, warning = FALSE}
library(tidyverse)
data <- read_csv("data.csv")

fifa <- data %>%
  select(ID, Name, Age, Club, Value, Wage, `Body Type`, Position, Joined, Height, Weight, BallControl, Dribbling, Strength, SprintSpeed, Acceleration, Interceptions, Stamina, Vision, Marking, Balance)

```

Selena's Part:

Matthew's Part:

Scott's Part:
